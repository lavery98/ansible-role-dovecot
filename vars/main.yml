---
# vars file for dovecot
_dovecot_packages:
  Debian:
    - dovecot-core
    - dovecot-imapd
    - dovecot-pop3d
    - dovecot-lmtpd
    - dovecot-mysql
    - dovecot-sieve
    - dovecot-managesieved

dovecot_packages: "{{ _dovecot_packages[ansible_os_family] }}"
